# Throw away uncommitted changes to a file, back to the last commit
git restore <file>

# Temporarily look at an older commit
git checkout <commit-id>

# Edit the message (or add forgotten files) on your last commit
git commit --amend
