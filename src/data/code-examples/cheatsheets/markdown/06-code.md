Inline code uses single backticks: `const x = 1;`

Fenced code block - add a language after the opening fence for syntax highlighting:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
```

```bash
npm install react-router-dom
```

An indented block (4 spaces) also works, but fenced blocks are preferred
since they support a language tag:

    console.log("indented code block");
