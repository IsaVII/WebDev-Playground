Horizontal rule - three or more hyphens, asterisks, or underscores on their own line:

---

***

___

Line break inside a paragraph - end the line with two trailing spaces,
then start the next line immediately below it.

Or use an explicit HTML break when trailing spaces are hard to see in an editor:
First line<br>
Second line

A blank line always starts a new paragraph without any special syntax.
