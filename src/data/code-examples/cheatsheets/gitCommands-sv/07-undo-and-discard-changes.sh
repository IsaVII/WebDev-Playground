# Kasta bort ostage:ade ändringar i en fil, tillbaka till senaste commit
git restore <file>

# Titta tillfälligt på en äldre commit
git checkout <commit-id>

# Redigera meddelandet (eller lägg till glömda filer) på din senaste commit
git commit --amend
