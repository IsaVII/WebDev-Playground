Kod i löptext använder enkla backticks: `const x = 1;`

Kodblock (fenced) - lägg till ett språk efter den öppnande fence:en för syntax-färgning:

```js
function greet(name) {
  return `Hello, ${name}!`;
}
```

```bash
npm install react-router-dom
```

Ett indenterat block (4 mellanslag) fungerar också, men fenced block är att
föredra eftersom de stödjer en språktagg:

    console.log("indenterat kodblock");
